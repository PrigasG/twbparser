% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_api.R
\name{tbs_custom_sql_graphql}
\alias{tbs_custom_sql_graphql}
\title{Custom SQL (Metadata API) for a published workbook/datasource}
\usage{
tbs_custom_sql_graphql(
  content_id,
  base_url = Sys.getenv("TABLEAU_BASE_URL"),
  site = Sys.getenv("TABLEAU_SITE"),
  token = Sys.getenv("TABLEAU_PAT")
)
}
\arguments{
\item{content_id}{Character. Workbook or datasource ID (GUID).}

\item{base_url}{Character. Tableau Server/Cloud base URL (e.g., "https://...").}

\item{site}{Character. Site contentUrl ("" for the default site).}

\item{token}{Character. REST credentials token (X-Tableau-Auth).}
}
\value{
A tibble with columns such as \code{custom_sql_name}, \code{custom_sql_query},
\code{database}, \code{schema}. Zero rows if none or if credentials are missing.
}
\description{
Queries Tableau's Metadata (GraphQL) API for Custom SQL tables under a content item.
}
\examples{
\dontrun{
tbs_custom_sql_graphql(
  content_id = "abc-123",
  base_url   = Sys.getenv("TABLEAU_BASE_URL"),
  site       = Sys.getenv("TABLEAU_SITE"),
  token      = "<token>"
)
}
}
