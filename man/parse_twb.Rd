% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_twb.R
\name{parse_twb}
\alias{parse_twb}
\title{Parse a Tableau workbook and write a batch export to disk}
\usage{
parse_twb(path, output_dir = "results", overwrite = FALSE, quiet = FALSE)
}
\arguments{
\item{path}{Path to a \code{.twb} or \code{.twbx} file.}

\item{output_dir}{Directory to write outputs into. Created if needed
(including parents).}

\item{overwrite}{If \code{FALSE} (default), refuse to write into an existing
non-empty directory instead of mixing outputs. If \code{TRUE}, parse_twb's own
previous output files in the directory are removed first so the export
reflects the current workbook; unrelated files are left alone.}

\item{quiet}{If \code{TRUE}, suppress progress messages.}
}
\value{
The normalized \code{output_dir}, invisibly.
}
\description{
\code{parse_twb()} is a convenience wrapper for non-interactive use: it parses a
\code{.twb}/\code{.twbx} workbook with \link{TwbParser} and writes a structured set of
outputs into \code{output_dir} — a human-readable report, one CSV per key table
(including the rebuild kit: unused fields, calculation build order, and
parameter usage),
per-worksheet visualization specs (\code{sheet_specs.txt}), a plain-text
replication brief, and the field dependency graph as GraphML
(readable with \code{igraph}/\code{ggraph} or any GraphML tool).
}
\examples{
twb <- system.file("extdata", "test_for_wenjie.twb", package = "twbparser")
if (nzchar(twb) && file.exists(twb)) {
  out <- parse_twb(twb, output_dir = file.path(tempdir(), "twbparser-demo"),
                   quiet = TRUE)
  list.files(out)
}
}
