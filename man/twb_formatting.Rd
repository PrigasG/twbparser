% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formatting.R
\name{twb_formatting}
\alias{twb_formatting}
\title{Formatting rules (fonts, colours, number formats, …)}
\usage{
twb_formatting(x, scope = NULL)
}
\arguments{
\item{x}{A \code{TwbParser} object or an \code{xml2} document.}

\item{scope}{Optional character scalar (\code{"worksheet"}, \code{"dashboard"}, or
\code{"workbook"}) to restrict output.}
}
\value{
A tibble with columns:
\describe{
\item{scope}{\code{"worksheet"}, \code{"dashboard"}, or \code{"workbook"}.}
\item{container}{Worksheet or dashboard name (\code{NA} for workbook scope).}
\item{element}{The styled element, e.g. \code{"mark"}, \code{"axis"}, \code{"pane"},
\code{"cell"}, \code{"title"}.}
\item{field}{Field the rule targets, if any; \code{NA} otherwise.}
\item{target}{Palette bucket or other target value, if any; \code{NA}
otherwise.}
\item{attr}{Formatting attribute, e.g. \code{"font-family"}, \code{"font-size"},
\code{"color"}, \code{"number-format"}, \code{"encoding-color"}.}
\item{value}{Attribute value.}
}
}
\description{
Walks every \verb{<format>} entry inside a \verb{<style-rule>} and returns a tidy
table of formatting attributes. This is where fonts, colours, number/date
formats, alignment, and shading live in a Tableau workbook. Filter the
\code{attr} column for what you need (e.g. \code{attr == "number-format"}, or
\code{startsWith(attr, "font")}).
}
\examples{
xml <- xml2::read_xml(
  '<workbook><worksheets><worksheet name="Sales"><table><style>
    <style-rule element="mark">
      <format attr="mark-labels-cull" value="true"/>
      <format attr="number-format" value="$#,##0"/>
    </style-rule>
  </style></table></worksheet></worksheets></workbook>'
)
twb_formatting(xml)

}
