# Per-dashboard summary (filters count and chart types).

Per-dashboard summary (filters count and chart types).

## Usage

``` r
twb_dashboard_summary(x)
```

## Arguments

- x:

  TwbParser or xml2 document.

## Value

Tibble with columns: dashboard, worksheet_count, zone_count, filters,
chart_types.
