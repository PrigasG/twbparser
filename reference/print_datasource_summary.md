# Print a quick data-source summary from a parser object

Print a quick data-source summary from a parser object

## Usage

``` r
print_datasource_summary(parser)
```

## Arguments

- parser:

  An object with `get_datasources()`, `get_parameters()`,
  `get_datasources_all()`

## Value

Invisibly prints summary
